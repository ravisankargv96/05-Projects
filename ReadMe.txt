Note: This repo name is a placeholder, change it later
Instead of generic sql model script, 
Generate scripts based on above created tables.

Currently script is written purely for testing purpose

Since the database tables might get upgraded or altered later. I need a version management to track the database tables

Use this repo in writing detailed of DNProjectSetup steps
